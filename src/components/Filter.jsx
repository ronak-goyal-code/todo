import React from 'react'

function Filter() {
  return (
    <div>
      Filter
      Added a filter component to the project. This component will allow users to filter the list of items based on certain criteria. The filter options will be displayed as checkboxes, and users can select multiple options to narrow down their search results. The filter component will also include a reset button to clear all selected filters and show the full list of items again.

    </div>
  )
}

export default Filter